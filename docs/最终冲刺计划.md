# 最终冲刺计划

> 日期：2026-06-19
> 评测脚本优化到此为止，精力转回作品本身

---

## 当前状态

```
✅ 文本问答 + 证据链     完成度 100%
✅ 主动选择景点/路线     完成度 100%
✅ FAQ L3 语义匹配       完成度 100% (34题)
✅ 知识盲区聚合与管理    完成度 100%
✅ Qwen 图片识别闭环     完成度 100% (confidence=0.95)
✅ 前端图片上传入口      完成度 100%
✅ 前端录音骨架          完成度 100%
✅ 管理后台 5 页         完成度 90%
✅ 延迟优化              完成度 100% (30s→1.7s)
✅ 置信度→情感映射       完成度 100%
✅ 讲解模式选择器        完成度 100%
⚠️ 数字人口型同步       CSS动画循环，非真实TTS驱动
❌ 真实语音 ASR/TTS       stub占位，缺账号
❌ 演示流程固化           未做
```

---

## 剩下三件事（按顺序）

### 1. Duix-Avatar 开源数字人接入

直接把现在手写的 `AvatarDisplay.vue` + `useAvatar.js` 替换为成熟的 Vue 组件。

**为什么选 Duix-Avatar**：
- MIT 许可证，比赛可直接用
- Vue 组件，零学习成本
- 内置音频→口型同步（现在我们最缺的）
- npm 安装，不改后端一行代码

**改动**：
```
npm install duix-avatar
```

删除：
- `frontend/src/components/AvatarDisplay.vue`
- `frontend/src/composables/useAvatar.js`

修改：
- `frontend/src/views/tourist/ChatView.vue`：用 `<DuixAvatar>` 替换现有 Avatar 区域
- `frontend/src/stores/chat.js`：avatar SSE 事件数据喂给 Duix

保留不变：
- 后端 SSE 协议不动
- `api/admin.js` 的 avatar API 不动（切换形象）
- `views/admin/AvatarConfig.vue` 不动（后台配置页）

**验收**：发送问题 → 数字人说话有真实口型同步 → 生产构建通过。

---

### 2. 演示脚本准备（7 分钟）

比赛演示时间有限，必须是高密度亮点展示，不能自由聊天。

**建议流程**：

| 时间 | 内容 | 演示什么 |
|------|------|------|
| 0:00 | 页面加载，数字人待机呼吸动画 | E 数字人 |
| 0:30 | 选择"灵山大佛"景点，问"大佛有多高" | A FAQ快路径 + 证据 |
| 1:00 | 问"玄奘为什么叫它小灵山" | B RAG+DeepSeek生成 |
| 1:30 | 切"儿童模式"，问同一问题 | B 讲解模式切换 |
| 2:00 | 问"有什么推荐的路线" | A 路线规划 |
| 2:30 | 拍照识景（灵山大佛图）→ 确认候选 → 一键提问 | B 多模态 |
| 3:30 | 问"九龙灌浴门票多少钱" | A 盲区拒答 |
| 4:00 | 连续追问"那下午几点开始" | A 追问上下文 |
| 4:30 | 切到管理后台，展示盲区管理+知识CRUD | C 管理闭环 |
| 5:30 | 展示数据大屏 | C 数据可视 |
| 6:00 | 回到游客端，总结 | 收尾 |

如果需要更短（5 分钟），砍掉路线规划和管理后台部分。

```text
写一个演示脚本放在 docs/demo_script.md，包含每步的提问文本、预期回答和讲解词。
```

---

### 3. 演示准备清单（阶段 F）

| 事项 | 说明 |
|------|------|
| 前端生产构建 | `npm run build` 成功 |
| 后端完整测试 | `pytest` 50 passed |
| 环境变量就绪 | `.env` 中 DeepSeek + Qwen 配置正确 |
| 预热流程 | 启动后端后先问一个问题触发模型加载 |
| 备用方案 | 如果网络断 → FAQ+选择+stub全链路仍可演示 |
| 播放设备 | 确认电脑有声音输出（数字人TTS需要） |
| 录屏备份 | 提前录一段 7 分钟演示视频，防现场翻车 |

---

## 不做

- ❌ 不再调评测脚本参数
- ❌ 不接真实 ASR/TTS（需账号，来不及）
- ❌ 不开发新功能模块（路线引擎、对话树、问题分类器）
- ❌ 不修改数据库 schema
- ❌ 不新增 Python 依赖

---

## 时间分配

```
Duix-Avatar 接入: 2 小时 (Claude)
演示脚本编写:   30 分钟 (Claude)
前端构建验证:   10 分钟
后端回归测试:   5 分钟
录屏演示:       20 分钟
```

**总计约 3 小时，今天下午可以全部完成。**
