# 数字人方案实施落地检测报告

> 检测时间：2026-06-20  
> 检测范围：根据 3 份方案文档（数字人开发执行方案 / 形象多样化方案），对 7 个前端源文件逐行比对

---

## 一、总评分：60/100

```
████████████░░░░░░░░ 60% 方案已落地

已完成（可直接验收）：
  ✅ AvatarConfig.vue 管理页         95%
  ✅ ChatView 形象配置读取接入       90%
  ✅ useAudioPlayer 增强             70%
  ✅ useModelManager 结构搭建        60%

未完成（核心口型链路断裂）：
  ❌ Live2DAvatar 口型同步改造      10%
  ❌ chat.js → ChatView 的 onProgress 管道   30%
  ❌ useAvatar 表情扩展             30%
  ❌ useModelManager 模型URL修正    20%
  ❌ Live2DAvatar idle动画/呼吸眨眼  0%
  ❌ 动作 Motion 系统                0%
```

---

## 二、逐文件对比详情

### 2.1 AvatarConfig.vue — 管理端配置页 ⭐ 95%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| 3 张模型卡片 + 选中高亮 | ✅ 有 `model-grid` + `model-card` + `.active` | ✅ |
| 实时预览 Live2DAvatar | ✅ `<Live2DAvatar :model-key="selectedModel" .../>` | ✅ |
| 表情测试按钮（空闲/微笑/致歉） | ✅ 有 3 个 emotion 按钮 | ✅ |
| 模拟说话开关 | ✅ `previewSpeaking` toggle | ✅ |
| 语音音色选择器 | ✅ 4 种音色 + speed/volume 滑块 | ✅ |
| 服饰配色区 | ❌ 未在代码中出现 | ❌ |
| localStorage 保存/恢复 | ✅ STORAGE_KEY 持久化 | ✅ |
| 重置按钮 | ✅ 恢复默认 + clear storage | ✅ |

**差距**：缺少方案中的"服饰配色"section（方案标注为"计划中"，可接受）。

---

### 2.2 useModelManager.js — 模型管理器 ⚠️ 60%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| 3 个不同 CDN URL 的模型 | ❌ **shizuku/hibiki/haru 全部指向同一个 CUBISM4_MODEL_URL** | ❌ |
| 导入路径切换为基础导入 | ❌ 仍用 `pixi-live2d-display/cubism4` | ❌ |
| switchModel 方法 | ✅ destroy → from() → addChild | ✅ |
| getModelList / getCurrentModelInfo | ✅ 结构完整 | ✅ |

**关键问题**：
```javascript
// useModelManager.js 第 3-4 行
const CUBISM4_MODEL_URL = "...haru_greeter_t03.model3.json";
// 第 8、15、22 行——三个模型全指向这个URL！
// 所以不论选哪个"形象"，实际加载的是同一个模型
```

---

### 2.3 Live2DAvatar.vue — 核心组件 ❌ 35%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| modelKey prop + switchModel 暴露 | ✅ 新增 `modelKey` prop、`handleSwitchModel`、`defineExpose` | ✅ |
| viseme 驱动口型（visemeTimeline 插值） | ❌ 未新增 `visemeTimeline` prop | ❌ |
| speechProgress prop | ❌ 未新增 | ❌ |
| visemeTimeline 插值算法 | ❌ 代码中不存在 `applyVisemeFromTimeline` | ❌ |
| 兜底正弦波口型 | ⚠️ 50ms 间隔随机化（非方案要求），有进步但不达标 | ⚠️ |
| 呼吸动画（ParamBreath） | ❌ 不存在 | ❌ |
| 眨眼动画（ParamEyeLOpen/ROpen） | ❌ 不存在 | ❌ |
| 7 种表情映射 | ❌ 仍有只有 3 种（f00/f01/f04） | ❌ |
| 空闲时呼吸+眨眼 | ❌ 不存在 | ❌ |
| onProgress 回调对接 | ❌ 不存在 | ❌ |

**当前口型代码**（第 147-173 行）：
```javascript
// 仍然是旧的 50ms 间隔 toggle，只是加了随机化
mouthTimer = setInterval(() => {
  if (frameCount % Math.floor(Math.random() * 5 + 8) === 0) {
    targetOpen = Math.random() * 0.6 + 0.2;  // ← 随机化不是口型同步
  }
  currentOpen += (targetOpen - currentOpen) * 0.3;
  model.internalModel.coreModel.setParameterValueById("ParamMouthOpenY", currentOpen);
  frameCount += 1;
}, 50);
```

**而方案要求的是**：
```javascript
// 按 viseme_timeline 线性插值
function applyVisemeFromTimeline(timeline, elapsedMs) { ... }
// 兜底才用正弦波
function applyFallbackMouth(elapsedMs) { ... }
```

---

### 2.4 useAudioPlayer.js — TTS 播放器 ⚠️ 70%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| playAudioBuffer 接收 onProgress 回调 | ❌ `playAudioBuffer` 签名无 onProgress | ❌ |
| 30ms 进度回调定时器 | ❌ 不存在 | ❌ |
| enqueue 第4参数为 onProgress | ❌ 第4参数是 `text`（给 TTS 兜底用的） | ❌ |
| currentProgress / currentDuration 暴露 | ❌ 不存在 | ❌ |
| **新增**：SpeechSynthesis 浏览器 TTS 兜底 | ✅ 新增 `playSpeechSynthesis(text)`，自动选中文语音 | ✅ |
| **新增**：stop() 方法 | ✅ 可取消当前语音 | ✅ |

---

### 2.5 chat.js — SSE 事件处理 ⚠️ 40%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| audio handler 传 onProgress | ❌ 签名是 `(base64, duration_ms)` 未读 `viseme_timeline` | ❌ |
| enqueue 传 onProgress 回调 | ❌ 传的是 `text` 参数 | ❌ |
| avatar handler 不变 | ✅ 保持 `viseme_text, emotion` | ✅ |
| **新增**：onAudioStart | ✅ 新增 `options.onAudioStart?.()` | ✅ |

```javascript
// 当前 chat.js 第 73-79 行（未按方案改造）
audio: async ({ base64, duration_ms }) => {
  const currentText = assistantMessage.content;
  options.onAudioStart?.();
  await audioPlayer.enqueue(base64, duration_ms, () => {
    options.onAudioEnded?.();
  }, currentText);   // ← 第4参数是 text，不是 onProgress
},
```

**方案要求**：
```javascript
audio: async ({ base64, duration_ms, viseme_timeline }) => {
  await audioPlayer.enqueue(base64, duration_ms, 
    () => options.onAudioEnded?.(),
    (progress, elapsedMs) => options.onSpeechProgress?.(progress, elapsedMs, viseme_timeline || null)
  );
},
```

---

### 2.6 ChatView.vue — 游客端主页面 ⚠️ 55%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| speechProgress ref | ❌ 未新增 | ❌ |
| visemeTimeline ref | ❌ 未新增 | ❌ |
| Live2DAvatar :speech-progress | ❌ 未传 | ❌ |
| Live2DAvatar :viseme-timeline | ❌ 未传 | ❌ |
| Live2DAvatar :model-key | ✅ 从 avatarConfig 读取并传入 | ✅ |
| avatarConfig localStorage 读取 | ✅ 已实现 | ✅ |
| submitQuery 中 onSpeechProgress | ❌ 不存在 | ❌ |
| **新增**：onAudioStart | ✅ `options.onAudioStart` → `avatar.setState("speaking")` | ✅ |
| import DEFAULT_MODEL_KEY | ✅ 第 302 行 | ✅ |

---

### 2.7 useAvatar.js — 状态管理 ⚠️ 30%

| 计划要求 | 现状 | 状态 |
|----------|------|:--:|
| 7 种情感映射 | ❌ 仍只有 happy/apology/neutral | ❌ |
| sad→apology, surprised→thinking 等映射 | ❌ 不存在 | ❌ |

---

## 三、整体架构差距图

```
                   方案预期                       实际落地
                   ────────                       ────────

音频播放进度  ───→ useAudioPlayer.onProgress  ──→ (断裂) 无 onProgress
                    ↓
ChatView       ───→ speechProgress ref        ──→ (缺失) 未创建
                    ↓
Live2DAvatar   ───→ visemeTimeline 插值       ──→ (缺失) 仍是随机 toggle
                    ↓
口型同步        ───→ ParamMouthOpenY 精确驱动  ──→ (伪同步) Math.random

形象切换        ───→ 3 个不同模型 CDN URL      ──→ (伪切换) 全部同一 URL
                    ↓
AvatarConfig   ───→ 模型卡片 + 预览 + 保存    ──→ ✅ 已完成

表情系统        ───→ 7 种 Live2D 表情         ──→ (缺失) 仍 3 种

动作系统        ───→ Motion + 随机微动        ──→ (缺失) 未启动
```

---

## 四、修正优先级

### 🔴 P0 — 阻塞性（不做口型同步=白做）

| 任务 | 文件 | 工时估计 |
|------|------|----------|
| `useAudioPlayer` 加 onProgress | `useAudioPlayer.js` | 15min |
| `Live2DAvatar` 加 speechProgress/visemeTimeline props + 插值/兜底算法 | `Live2DAvatar.vue` | 45min |
| `chat.js` audio handler 改签名 | `chat.js` | 5min |
| `ChatView` 加 refs + props 绑定 + onSpeechProgress | `ChatView.vue` | 15min |

### 🟡 P1 — 核心体验

| 任务 | 文件 | 工时估计 |
|------|------|----------|
| `useModelManager` 3 个不同 URL | `useModelManager.js` | 10min |
| `useAvatar` 7 种表情映射 | `useAvatar.js` | 10min |
| `Live2DAvatar` 空闲呼吸+眨眼 | `Live2DAvatar.vue` | 20min |

### 🟢 P2 — 锦上添花

| 任务 | 文件 | 工时估计 |
|------|------|----------|
| Motion 动作系统 | `Live2DAvatar.vue` | 30min |
| AvatarConfig 服饰配色 section | `AvatarConfig.vue` | 10min |

---

## 五、一句话结论

> **形象管理端 UI 和模型切换架构已就绪，但核心的口型同步链路从 `useAudioPlayer → chat.js → ChatView → Live2DAvatar` 全程断裂，数字人目前仍以随机频率张嘴——这是当前最大的差距，也是赛题评分最核心的限制项。**
